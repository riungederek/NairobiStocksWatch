import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.nairobistockswatch',
  appName: 'NairobiStocksWatch',
  webDir: 'dist'
};

export default config;
